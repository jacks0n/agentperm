{
  "version": 1,
  "permissions": {
    "allow": [
      "Shell(uv run ruff check)",
      "Shell(ruff check)",
      "Shell(uv run python -m pytest)",
      "Shell(uv run pytest)",
      "Shell(pytest)",
      "Shell(uv run basedpyright)",
      "Shell(basedpyright)",
      "Shell(uv run agentperm validate)",
      "Shell(agentperm validate)",
      "Shell(agentperm why)",
      "Shell(python -m pytest)",
      "Shell(kiro-cli agent validate)",
      "Shell(actionlint !-*)",
      "Shell(just {build,check})",
      "Shell(mise exec -- <EXEC>)",
      "Shell(cargo check)",
      "Shell(cargo test)",
      "Shell(cargo build)",
      "Shell(rustup which)",
      "Shell(.venv/bin/{pytest,ruff,basedpyright})"
    ]
  }
}
