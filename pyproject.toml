[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentperm"
version = "0.4.0"
description = "Permission policy mediator for Claude Code, Codex, OpenCode, Gemini CLI, and Kiro."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"
authors = [{ name = "Jackson Cooper", email = "jackson@jacksonc.com" }]
keywords = [
    "claude-code",
    "codex-cli",
    "gemini-cli",
    "opencode",
    "kiro",
    "coding-agent",
    "ai-agents",
    "permissions",
    "policy-engine",
    "hooks",
    "tree-sitter-bash",
    "llm",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Security",
    "Topic :: Software Development",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Shells",
    "Topic :: System :: Systems Administration",
    "Typing :: Typed",
]
dependencies = [
    "pyjson5>=2.0,<3",
    "tomlkit>=0.13,<0.14",
    "tree-sitter>=0.25,<0.26",
    "tree-sitter-bash>=0.25,<0.26",
]

[project.urls]
Homepage = "https://github.com/jacks0n/agentperm"
Repository = "https://github.com/jacks0n/agentperm"
Documentation = "https://github.com/jacks0n/agentperm/tree/main/docs"
Changelog = "https://github.com/jacks0n/agentperm/blob/main/CHANGELOG.md"
Issues = "https://github.com/jacks0n/agentperm/issues"

[project.scripts]
agentperm = "agentperm:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
    "basedpyright>=1.13",
]

[tool.hatch.build.targets.wheel]
packages = ["src/agentperm"]

[tool.hatch.build.targets.sdist]
include = ["src/", "tests/", "README.md", "LICENSE", "pyproject.toml"]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.basedpyright]
include = ["src", "tests"]
pythonVersion = "3.12"
typeCheckingMode = "strict"
# tree-sitter-bash stubs are partial. We narrow at the parser boundary so domain
# code never sees Unknown.
reportMissingTypeStubs = false
reportUnknownMemberType = false
reportUnknownVariableType = false
reportUnknownArgumentType = false

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
